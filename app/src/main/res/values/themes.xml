<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CheburGate" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="android:windowBackground">@color/background_light</item>
        <!-- Android 15+ (API 35) enforces edge-to-edge by default — opt out -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>

    <style name="Theme.CheburGate.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="Theme.CheburGate.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
</resources>
